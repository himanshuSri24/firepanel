{
  "name": "firepanel",
  "version": "2.0.0",
  "private": true,
  "description": "Search every list, filter fields by key or value, and copy whole documents as JSON. Read-only. Not affiliated with Google.",
  "scripts": {
    "icons": "node scripts/generate-icons.mjs",
    "build": "vite build && node scripts/build-manifest.mjs",
    "dev": "vite build --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "package": "npm run icons && npm run build && cd dist && zip -qr ../firepanel.zip . && cd .. && echo 'firepanel.zip ready'"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.42",
    "@types/node": "^26.6.1",
    "jsdom": "^29.1.1",
    "prettier": "^3.9.8",
    "typescript": "^5.9.3",
    "vite": "^5.4.21",
    "vitest": "^4.1.11"
  },
  "productName": "Firepanel \u2014 tools for the Firestore console",
  "license": "MIT",
  "homepage": "https://github.com/himanshuSriv24/firepanel"
}
